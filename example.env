# You can use this file to set environment variables for your project.
# This file is a template and should not be used directly. Instead, copy it to .env and set the values there.

# compose file used
COMPOSE_FILE=docker-compose.yml

# dbt env vars
DBT_PROJECT_DIR=dbt_kpis
DBT_TARGET=dev

# dbt connection env vars used in container
SOMENERGIA_KPIS_DB_HOST=dbdades.somenergia.lan
SOMENERGIA_KPIS_DB_PORT=5432
SOMENERGIA_KPIS_DB_USER=changeme
SOMENERGIA_KPIS_DB_PASSWORD=changeme
SOMENERGIA_KPIS_DB_DBNAME=mercat_test